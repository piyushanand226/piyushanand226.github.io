# E2E Setup (Playwright)

## Install

```bash
npm install
npx playwright install chromium
```

## Run

```bash
npm run test:e2e
```

This suite starts a local preview server on `http://127.0.0.1:4173` using the Playwright `webServer` setting.
